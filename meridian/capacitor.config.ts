const config = {
  appId: "net.debtmarket.meridian",
  appName: "Meridian",
  webDir: "out",
  ios: {
    contentInset: "automatic",
    preferredContentMode: "mobile",
    scrollEnabled: true,
  },
};

export default config;
