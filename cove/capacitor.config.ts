const config = {
  appId: "net.debtmarket.cove",
  appName: "Cove",
  webDir: "out",
  ios: { contentInset: "automatic", preferredContentMode: "mobile" },
  android: { allowMixedContent: false },
};

export default config;
